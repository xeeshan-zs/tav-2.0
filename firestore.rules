rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Testimonials - public read, anyone can write
    match /testimonials/{testimonial} {
      allow read: if true;
      allow create: if true;
      allow update, delete: if false;
    }
  }
}
